spring:
  application:
    name: rest-with-spring-boot-java

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/rest_with_spring_boot_java?serverTimezone=UTC&useSSL=false
    username: root
    password: 1122
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
    show-sql: false
    open-in-view: false
logging:
   level:
     #root: WARN
     br.com.allysson: DEBUG

